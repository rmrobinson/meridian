# Android

Android app for Meridian.

## Build

```bash
./gradlew assembleDebug
```

## Proto codegen

Proto generation for Android (Kotlin/gRPC) is deferred — add buf plugin when Android development begins.
