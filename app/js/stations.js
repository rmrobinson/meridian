/**
 * stations.js — Event dot rendering + hover/click targets.
 *
 * Renders <circle> elements for point events and span endpoints.
 * Each station has an invisible hit-area overlay sized to at least 44×44px
 * for accessible touch targets (per WCAG / Apple HIG guidance).
 */

// TODO: implement in Phase 1 rendering step.
