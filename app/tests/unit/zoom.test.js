import { describe, it, expect } from 'vitest';
// import { aggregateByMonth, filterForYearZoom } from '../../js/zoom.js';

// TODO: implement in Phase 2 alongside the zoom aggregation logic.
describe.todo('aggregateByMonth() — Phase 2');
// - Groups point events correctly by (family_id, year-month)
// - Aggregation label reflects correct count ("8 runs", "3 books finished")
// - Span events are never included in aggregation
// - Events from different families in the same month produce separate aggregates

describe.todo('filterForYearZoom() — Phase 2');
// - All point events are removed
// - Span events are retained
// - Each retained span includes a synthetic midpoint station
