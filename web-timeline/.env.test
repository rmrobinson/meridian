# Used by `npx vite --mode test` (the Playwright test server).
# Clears BACKEND_URL so Vite serves the mock fixture instead of proxying
# to a real backend, even when .env.local sets BACKEND_URL for the dev server.
BACKEND_URL=
