import { test, expect } from '@playwright/test';

test.describe('Responsive transitions', () => {
  test.fixme('resizing viewport from 1280px to 390px switches card presentation from floating to bottom sheet', async () => {});
  test.fixme('resizing from 390px to 1280px shows full concurrent sibling lanes (not collapsed)', async () => {});
});
