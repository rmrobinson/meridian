{
  "name": "@meridian/mcp",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "proto": "cd ../proto && buf generate",
    "build": "npm run proto && tsc",
    "start": "node dist/src/index.js",
    "dev": "node --import dotenv/config --import tsx/esm src/index.ts"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.11.0",
    "@modelcontextprotocol/sdk": "latest",
    "dotenv": "^16.0.0",
    "nice-grpc": "latest",
    "nice-grpc-common": "latest",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
